# AISO.Reporting

Placeholder module (modular monolith). Reserved for reporting/export features
(e.g. sales-order PDF/Excel generation) — to be implemented in a later sprint.
Intentionally empty for now; kept in the solution so the module boundary exists.