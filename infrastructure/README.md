# infrastructure/

Deployment and infrastructure-as-code assets for AISO-Teams.

- `docker/` — container/compose definitions (planned: relocate backend dev `docker-compose.yml` here).
- `ci/` — CI/CD pipeline configuration (note: the active GitHub Actions workflows live in `.github/workflows/`).

Placeholder for now; populated as deployment work begins (Sprint 4+).