{
  "name": "GetOrderDetail",
  "description": "Get full details of a specific SAP Sales Order. Requires explicit order_id.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "SAP Sales Order number."
      }
    },
    "required": ["order_id"],
    "additionalProperties": false
  }
}
