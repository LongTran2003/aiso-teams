{
  "name": "ForwardOrder",
  "description": "Forward a sales order to another user. Requires both order_id and forward_to_user.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "Sales order number."
      },
      "forward_to_user": {
        "type": "string",
        "description": "Recipient name/email, including honorifics as-is."
      }
    },
    "required": ["order_id", "forward_to_user"],
    "additionalProperties": false
  }
}
