{
  "name": "GetPendingApprovals",
  "description": "List PENDING release-approval requests in the manager inbox (maker-checker). Use ONLY when the user asks for pending approvals, approval inbox, orders awaiting approval, or waiting for manager approval. Do NOT use GetSalesOrders for these phrases.",
  "parameters": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  }
}
