{
  "name": "RejectOrder",
  "description": "Reject a sales order. Requires both order_id and reason_code.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "Sales order number."
      },
      "reason_code": {
        "type": "string",
        "enum": ["PRICE_ISSUE", "OUT_OF_STOCK", "OTHER"],
        "description": "Rejection reason. Enum: PRICE_ISSUE, OUT_OF_STOCK, OTHER."
      }
    },
    "required": ["order_id", "reason_code"],
    "additionalProperties": false
  }
}
