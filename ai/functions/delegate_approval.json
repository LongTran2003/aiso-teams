{
  "name": "DelegateApproval",
  "description": "Delegates the user's approval authority to another employee in SAP and local database.",
  "parameters": {
    "type": "object",
    "properties": {
      "delegateUser": {
        "type": "string",
        "description": "SAP User ID of the employee receiving the delegation."
      },
      "validFrom": {
        "type": "string",
        "format": "date",
        "description": "Start date of delegation in YYYY-MM-DD format."
      },
      "validTo": {
        "type": "string",
        "format": "date",
        "description": "End date of delegation in YYYY-MM-DD format."
      },
      "reason": {
        "type": "string",
        "description": "Optional reason for delegation."
      },
      "maxAmount": {
        "type": "number",
        "description": "Optional maximum order amount limit (e.g. 500000). If the user specifies a limit like 'dÆ°á»›i 50 triá»‡u' or 'háº¡n má»©c 500000', extract it here as a number."
      }
    },
    "required": [
      "delegateUser",
      "validFrom",
      "validTo"
    ]
  }
}