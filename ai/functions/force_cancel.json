{
  "name": "ForceCancel",
  "description": "Admin only: prepare force cancel for a sales order (bypasses ownership). Shows a confirm card with required override reason. Use when admin says 'force cancel' (not plain reject/cancel).",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "Sales order number."
      },
      "reason": {
        "type": "string",
        "description": "Optional draft override reason to prefill on the confirm card."
      }
    },
    "required": ["order_id"],
    "additionalProperties": false
  }
}
