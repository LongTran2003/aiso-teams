{
  "name": "CreateOrder",
  "description": "Opens the Create Sales Order UI form. ALWAYS call this function IMMEDIATELY when the user asks to create an order. DO NOT ask the user for any parameters (customer, material, etc.), just call this function with an empty object and the UI will handle it.",
  "parameters": {
    "type": "object",
    "properties": {},
    "additionalProperties": false
  }
}
