{
  "name": "ViewAuditLog",
  "description": "Show recent bot audit log entries. Admin only. Use when an admin asks for audit log, activity log, or who did what.",
  "parameters": {
    "type": "object",
    "properties": {
      "limit": {
        "type": ["integer", "null"],
        "description": "Number of recent entries (default 20, max 100)."
      }
    },
    "required": [],
    "additionalProperties": false
  }
}
