{
  "name": "UpdateOrderReference",
  "description": "Update the customer PO reference on a sales order. Requires order_id and new_reference.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "Sales order number."
      },
      "new_reference": {
        "type": "string",
        "description": "New PO reference number."
      }
    },
    "required": ["order_id", "new_reference"],
    "additionalProperties": false
  }
}
