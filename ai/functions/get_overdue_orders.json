{
  "name": "GetOverdueOrders",
  "description": "Get overdue SAP sales orders. All params optional.",
  "parameters": {
    "type": "object",
    "properties": {
      "customerIdOrName": {
        "type": ["string", "null"],
        "description": "Customer ID or partial name."
      },
      "salesOrg": {
        "type": ["string", "null"],
        "enum": ["TV01", "FU24", "UE00", "UW00", "DN00", "DS00", null],
        "description": "Sales org."
      },
      "daysPastDue": {
        "type": ["integer", "null"],
        "description": "Minimum days past due."
      },
      "top": {
        "type": ["integer", "null"],
        "description": "Max records to return."
      }
    },
    "required": [],
    "additionalProperties": false
  }
}
