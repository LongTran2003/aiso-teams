{
  "name": "ForceRelease",
  "description": "Admin only: prepare force release for a sales order (bypasses ownership). Shows a confirm card with required override reason. Use when admin says force release.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "Sales order number."
      },
      "reason": {
        "type": "string",
        "description": "Optional draft override reason to prefill on the confirm card."
      }
    },
    "required": ["order_id"],
    "additionalProperties": false
  }
}
