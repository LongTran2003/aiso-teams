{
  "name": "ApproveOrder",
  "description": "Approve a pending release request and release the sales order in SAP. Use when a manager says approve order, approve release, or confirm release request.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "Sales order number."
      },
      "comment": {
        "type": ["string", "null"],
        "description": "Optional approval comment."
      }
    },
    "required": ["order_id"],
    "additionalProperties": false
  }
}
