{
  "name": "ReleaseOrder",
  "description": "Directly release a sales order in SAP. Do NOT use for phrases like 'request release', 'submit for approval', or 'yêu cầu duyệt' — those must call RequestRelease. Prefer ApproveOrder when a pending release request exists.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "Sales order number."
      },
      "comment": {
        "type": ["string", "null"],
        "description": "Optional comment or note."
      }
    },
    "required": ["order_id"],
    "additionalProperties": false
  }
}
