{
  "name": "ReleaseOrder",
  "description": "Approve and release a sales order. Requires order_id.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "Sales order number."
      },
      "comment": {
        "type": ["string", "null"],
        "description": "Optional comment or note."
      }
    },
    "required": ["order_id"],
    "additionalProperties": false
  }
}
