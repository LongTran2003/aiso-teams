{
  "name": "CheckOrderStatus",
  "description": "Check status and basic info of a specific SAP Sales Order. Requires explicit order_id.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "SAP Sales Order number (e.g. '0000000009', 'ORD-202620011')."
      }
    },
    "required": ["order_id"],
    "additionalProperties": false
  }
}
