{
  "name": "RejectApproval",
  "description": "Reject a pending release-approval request (does not reject the sales order itself). Manager or Admin only.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "Sales order number."
      },
      "comment": {
        "type": ["string", "null"],
        "description": "Reason for rejecting the approval request."
      }
    },
    "required": ["order_id"],
    "additionalProperties": false
  }
}
