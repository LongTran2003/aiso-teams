# pdf-function/

Azure Function for server-side PDF generation (e.g. sales-order documents).

- `templates/` — document/HTML templates used to render PDFs.

Placeholder for now; implemented in a later sprint once the reporting flow is defined.